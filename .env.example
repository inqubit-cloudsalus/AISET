# AISET provider credentials — read from the environment only, never committed,
# never printed by the CLI (including `aiset doctor`, which reports presence only).
ANTHROPIC_API_KEY=''
OPENAI_API_KEY=''
# OpenRouter (provider: "openrouter"). OPENROUTER_API_KEY is accepted as an alias.
OPENROUTE_API_KEY=''
